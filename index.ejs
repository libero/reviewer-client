<!DOCTYPE html>
<html lang="en">
  <head>
    <title>eLife</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1,shrink-to-fit=no">
    <link rel="shortcut icon" type="image/x-icon" href="/assets/eLife.ico" />
    <script
    src="<%= htmlWebpackPlugin.files.chunks.config.entry %>"
    type="text/javascript" ></script>
  </head>
  <body>
      <div id="app">
      </div>
  </body>
  <%
    for (key in htmlWebpackPlugin.files.chunks) { %><%
      if (key !== 'config') { %>
        <script
        src="<%= htmlWebpackPlugin.files.chunks[key].entry %>"
        type="text/javascript"></script>
      <%}%>
  <%}%>
</html>
